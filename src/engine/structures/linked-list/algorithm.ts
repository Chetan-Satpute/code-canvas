import { algorithmFor, operationFor } from '../../algorithm.ts';
import { CoreLinkedList } from './structure.ts';

export const defineLinkedListAlgorithm = algorithmFor(CoreLinkedList);
export const defineLinkedListOperation = operationFor(CoreLinkedList);
