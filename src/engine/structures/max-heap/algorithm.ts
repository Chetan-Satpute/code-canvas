import { algorithmFor, operationFor } from '../../algorithm.ts';
import { CoreMaxHeap } from './structure.ts';

export const defineMaxHeapAlgorithm = algorithmFor(CoreMaxHeap);
export const defineMaxHeapOperation = operationFor(CoreMaxHeap);
